#!/usr/bin/env bash

set -e

function assert_root {
  if [[ $EUID -ne 0 ]]; then
     echo "This script must be run as root" 
     exit 1
  fi
}

function vbus_off {
  echo 0 > /sys/class/gpio/gpio5/value
}

assert_root
vbus_off
echo "OK"
